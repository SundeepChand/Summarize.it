def get_sentences(text):
    '''Return an array of all the sentences with a bool field, if it is a sentence or not'''
    pass


def summarize(text):
    is_sentences = get_sentences(text)
    summary_text = ''
    return summary_text


def summarize_text(input_text=''):
    summarized_text = summarize(input_text)
    summarized_text = 'Test: ' + input_text
    return summarized_text
